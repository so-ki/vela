# 实验① 结果：州级元数据完备率 —— 本轮判定【无效：仪器失效】

- 运行时间：2026-07-18T05:07:30Z（云端执行环境）
- 原始数据：`exp01_raw.json`；响应缓存：`cache/exp01/`

## 结论：本轮测得的"0 记录"不是覆盖率，是仪器失效

30/30 条 SRU 查询返回的都是**塞纳多联邦"Verificação de segurança"反爬挑战页**（见 `cache/exp01/` 任一缓存文件开头），不是 SRU XML。挑战为 JS 型：

- 纯 HTTP（含浏览器 UA 伪装）不可过；
- 无头 Chromium（经代理）连接被重置；
- 第三方云端抓取通道返回 503。

因此：**预登记判据（州级 ≥70%）在本轮没有得到检验，覆盖率未知**。按登记簿纪律，本轮不得触发"州级覆盖率"分支的任何设计裁决。

## 本轮的真实发现（可达性，本身是实验数据）

1. **LexML（Senado 托管）对云端出口的程序化访问被整体封锁**。"LexML API 可直接程序化接入"这一隐含假设至少要加限定语：*依赖出口网络环境*。Citator 的 A 层承诺措辞必须反映这一点（如"元数据同步为半自动，受来源可用性约束"）。
2. **ALESP（圣保罗州议会）官方法规仓库无挑战、直接可达**：Decreto 8.468/1976、Lei 997/1976、Lei 13.577/2009 整合全文均成功抓取（见实验④ `cache/exp04/`）。州级**全文**语料可得性独立于 LexML **元数据**可得性——两条通道要分开承诺。

## 后续动作（登记，不改判据）

- [ ] 团队成员在本地/住宅网络重跑本脚本（支持缓存与 `--offline` 重放，结果可审计），得到有效覆盖率后再回到预登记判据裁决 Citator v1 范围。
- [ ] 备选通道调研：LexML 官方数据转储 / OAI 接口离线获取。
- 判据本身**不动**：州级"URN 可解析且元数据存在"≥70% → Citator v1 含州级；否则仅联邦。
