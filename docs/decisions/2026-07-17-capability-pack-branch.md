# 决策记录：`feature/scope-confirmation-layer` 分支处置

- 日期：2026-07-17（Asia/Shanghai）
- 状态：已决策，等待私有仓库 PR 合并后的清理动作
- 决策类型：分支治理；不包含代码、能力包内容或接口冻结

## 事实证据

本地只读核对结果如下：

| 引用 | 提交 |
|---|---|
| `main` | `36074bad1ccdf237c15916c13131bda880ed9af0` |
| `feature/scope-confirmation-layer` | `36074bad1ccdf237c15916c13131bda880ed9af0` |
| `codex/vela-release-hardening`（记录时已提交的 HEAD） | `75cd80de72adeeea6772356248ef6009737d134b` |

前两个引用完全相同，因此旧 feature 指针不包含任何超出 `main` 的独立提交。当前交付分支为 `codex/vela-release-hardening`；该分支及其受控工作树已经吸收或替代原 scope-confirmation 工作的目标。表中的 hardening 提交只记录当时已提交的 HEAD，不把其后的未提交工作树变化宣称为已合并。最终吸收状态仍以私有仓库中的 PR 差异、测试和合并结果为准，不用未提交工作树代替合并证据。

## 决策

**现在不删除、不改写、也不强推 `feature/scope-confirmation-layer`。** 保留该旧指针，直到满足以下全部条件：

1. 远端仓库已由仓库所有者确认为私有；
2. `codex/vela-release-hardening` 的私有 PR 已完成审查并合并；
3. 合并后的默认分支验证通过，且 PR 中能够追溯 scope-confirmation 的替代实现；
4. 仓库所有者确认没有自动化、部署、保护规则或外部协作者依赖旧分支。

全部满足后，由仓库所有者删除本地和远端旧指针，并在 PR 或维护日志中记录删除时间。建议在 PR 合并后一个工作日内完成核对；这是一项事件触发的维护时限，不是假定的发布日期。

## 责任与验收

| 事项 | 负责人 | 验收证据 |
|---|---|---|
| 仓库私有化确认 | 仓库所有者 | GitHub 可见性设置及审计记录 |
| hardening PR 审查、测试与合并 | 发布负责人＋代码审查人 | 已合并 PR、必需检查通过 |
| 依赖旧分支核对 | 仓库所有者 | 分支保护、工作流、部署与协作者核对记录 |
| 旧指针删除 | 仓库所有者 | 远端分支列表／维护日志 |

在仓库所有者身份、私有化和 PR 合并证据齐备前，任何自动删除都不属于授权动作。

## 理由

- 旧分支与 `main` 没有内容差异，继续在其上开发只会制造错误的并行工作印象。
- 当前 hardening 分支承担了新的收敛与发布验证；后续变更应通过一个可审计的私有 PR 进入默认分支。
- 立即删分支是可避免的破坏性操作，且不能改善现有代码；等待合并后再清理既保留追溯性，也不妨碍开发。

## 触发重新决策的条件

只有出现以下事实之一才重开本决策：发现旧引用在其他远端含有未合并提交；发现部署／自动化硬依赖该分支；仓库所有者要求采用不同的长期分支策略。重开时必须附提交图或远端证据，不能仅凭分支名称推断。
